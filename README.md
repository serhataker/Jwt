# This project about jwt token ASP.CORE at the C# 
## When we used the token for the our controller services at the API we can use this pattern.
### We basicly used the a layered architecture at this project
